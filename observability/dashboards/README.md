# Observability Dashboards

Dashboard panels should consume the Prometheus metric names exposed by the
application. Suggested panels are API rate/latency, error rate, readiness,
PostgreSQL health, ingestion throughput, detection/correlation latency, queue
depth, alert rate, plugin health, and DLQ volume.
